describe("ASTFactory.ts",() => {
    describe("buildAST()",() => {
        it("builds a valid SubmissionAST if the provided string is valid Java code");
        it("throws an error if the provided string is not valid Java code");
    });
});