const ComparisonReducer = (state = [], action) => {
    switch (action.type) {
        case 'COMPARE':
            return //TODO;
        default:
            return state;
    }
};

export default ComparisonReducer;